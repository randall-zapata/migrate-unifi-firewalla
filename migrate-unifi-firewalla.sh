#!/bin/bash
# migrate-unifi-firewalla.sh (v1.11)
# See header in --help. Never deletes /data/unifi or jacobalberty image.
set -u -o pipefail
COMPOSE_DIR=/home/pi/.firewalla/run/docker/unifi
# Full script is in the companion download; this commit placeholder will be replaced.
# Run: bash migrate-unifi-firewalla.sh --help
echo "Use the v1.11 script from the repo raw URL after the full file lands."
exit 2
